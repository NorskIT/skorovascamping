{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "SvelteKit: dev",
			"type": "shell",
			"command": "if [ -s \"$HOME/.nvm/nvm.sh\" ]; then . \"$HOME/.nvm/nvm.sh\" && nvm use 24; fi; npm run dev -- --host 127.0.0.1",
			"windows": {
				"command": "$nodeCommand = Get-Command node -ErrorAction SilentlyContinue; $nodeDir = if ($nodeCommand) { Split-Path $nodeCommand.Source } else { Join-Path $env:LOCALAPPDATA 'Programs\\node-v24.18.0-win-x64' }; $env:Path = \"$nodeDir;$env:Path\"; & (Join-Path $nodeDir 'npm.cmd') run dev -- --host 127.0.0.1"
			},
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"isBackground": true,
			"problemMatcher": {
				"owner": "sveltekit-dev",
				"pattern": {
					"regexp": "^$"
				},
				"background": {
					"activeOnStart": true,
					"beginsPattern": ".*",
					"endsPattern": "Local:.*http://127\\.0\\.0\\.1:5173"
				}
			},
			"presentation": {
				"reveal": "silent",
				"panel": "dedicated",
				"clear": true
			}
		}
	]
}
